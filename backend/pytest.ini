[pytest]
# Tests import the backend packages directly (`from core...`, `from services...`),
# so put the backend root on sys.path rather than requiring an install step.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
