"""Does enforcing a JSON schema make Lumen's agents' replies better formed?

`python -m eval.structured.run --help` from `backend/`. Nothing here is imported
by the API: like `eval/`, it is a tool for judging the product, not part of it.
"""
