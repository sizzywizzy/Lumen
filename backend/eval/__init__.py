"""Offline evaluation of Lumen's audience simulator against real audience ratings.

`python -m eval.run --help` from `backend/`. Nothing here is imported by the
API: the evaluation is a tool for judging the product, not part of it.
"""
