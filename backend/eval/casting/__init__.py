"""Does Lumen's actor search surface the right actor for a part?

`python -m eval.casting.run --help` from `backend/`. Nothing here is imported by
the API: like `eval/`, it is a tool for judging the product, not part of it.
"""
