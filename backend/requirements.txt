# Core — enough to run the whole mock pipeline + API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Live integrations — uncomment as keys land (mock fallbacks work without them)
# google-genai>=1.0          # Gemini 2.0 Flash/Pro + Imagen (services/gemini_client.py)
# supabase>=2.4              # GlobalState persistence (services/supabase_client.py)
# tavily-python>=0.3         # web search for hype/PR agents
# langgraph>=0.2             # swap core/orchestrator/graph.py runner for a StateGraph
# openai-whisper             # or Whisper API for audition transcripts

# Google Cloud SQL (uncomment when using Cloud SQL instead of Supabase)
# google-cloud-sql>=3.0
# pymysql>=1.1

# Phase I actor knowledge base
psycopg[binary]>=3.2
requests>=2.32
sentence-transformers>=3.0
